# Tewodros Bejiga @02618567
list = [1,3,4,6,8,9,11,12,13,15,23,56,65,]  
def bSearch(list,target):
    low = 0
    high = len(list) -1
    while low <= high:
        mid = (low + high)/2
        if target == list[mid]:
            print  mid
            break
        elif target < list[mid]:
            high = mid - 1
        elif target > list[mid]:
            low = mid + 1
    else:
         return -1
